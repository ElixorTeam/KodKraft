---
import KodKraftLogo from "@/shared/assets/logo.svg";
import PixelChevronIcon from "@/shared/assets/icons/pixel-chevron.svg";
import { Button } from "@/shared/ui/button";
import { DialogTrigger } from "@/shared/ui/dialog";

const phone = "+7 (904) 066 06 06";
---

<header id="site-header" class="fixed top-0 z-40 w-screen transition-all">
  <div
    class="container mx-auto flex h-12 w-full items-center justify-between px-4 sm:px-6 md:h-16 lg:px-8"
  >
    <KodKraftLogo class="h-7 md:h-9" />

    <div class="inline-flex items-center md:gap-4">
      <Button variant="link" href={`tel:${phone}`} class="px-0 md:px-2">
        {phone}
      </Button>

      <DialogTrigger for="callback-dialog" asChild>
        <Button class="hidden h-8 font-semibold md:inline-flex">
          Записаться
          <PixelChevronIcon />
        </Button>
      </DialogTrigger>
    </div>
  </div>
</header>

<script>
  const header = document.getElementById("site-header");

  const handleScroll = () => {
    if (window.scrollY > 100) {
      header?.classList.add("backdrop-blur-md", "bg-background/85");
    } else {
      header?.classList.remove("backdrop-blur-md", "bg-background/85");
    }
  };

  window.addEventListener("scroll", handleScroll);

  handleScroll();
</script>
